<!--kwlist_nb2.jsp--><%/** * Suppose défini un bean Bean_KwList_Nb contenant la liste à afficher dans l'attribut de request "net.semanlink.servlet.Bean_KwList". */%><%@ page language="java" session="true" import="net.semanlink.servlet.*,net.semanlink.semanlink.*,java.util.*"%><%@ taglib uri="http://struts.apache.org/tags-bean" prefix="bean" %><%@ taglib uri="http://struts.apache.org/tags-html" prefix="html" %><%@ taglib uri="http://struts.apache.org/tags-logic" prefix="logic" %><%Bean_KwList_Nb truc = (Bean_KwList_Nb) request.getAttribute("net.semanlink.servlet.Bean_KwList");String contextPath = request.getContextPath();if (truc != null) {		int n = truc.size();		if (n > 0) {			%>				<%				for (int i = 0; i < n; i++) {					HTML_Link link = truc.getLink(i);
					/*int nb = truc.getNb(i);
					String nombre = null;					if (nb > 1) {						nombre = " (" + Integer.toString(nb) + ")";					} else {						nombre = "";					}*/
					String cla = "s" + truc.getNormalizedN(i);					/*if (nb < 6) {						cla = "s" + Integer.toString(nb);					} else {						cla = "s5";					}*/					// 2013-08 google nofollow rel="nofollow"					// 2020-02 TagAndTag					// String href = contextPath + link.getPage(); // 2020-02 tagcloud KO sur une page tag			        String href = link.getPage();			        if (!href.startsWith("http")) href = contextPath + href; // 2020-02 BEURK sinon tagcloud KO sur page news					%><a href="<%=response.encodeURL(href)%>" rel="nofollow" class="<%=cla%>"><%=link.getLabel()%></a> - <%				} // for				%>			<%		} // if (n > 0)} // if (truc != null)%><!--/kwlist2.jsp-->