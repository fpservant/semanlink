<%@ page
    contentType="text/html;charset=UTF-8" 
    pageEncoding="UTF-8"
%>
<style type="text/css"><!--.q {	font-weight: bold;}--></style><h1>FAQ</h1><div class="graybox"><h2>Is it possible to store documents outside the &quot;semanlinkDataDir&quot;?</h2><p>YES. It is possible to define several other &quot;DataFolders&quot;. This  is done using the &quot;<strong>semanlink-config.xml</strong>&quot; file located  in the <strong>[semanlinkDataDir]/conf</strong> directory.  Documentation about  this configuration file is <a href="config.htm">here</a>.</p><p>(The semanlinkDataDir is defined in the context of the web application, see  the READ-ME in the main directory of thedistribution)</p><h2>How can I use Semanlink on my website?</h2><p>Define one (or several) DataFolder in the &quot;[semanlinkDataDir]/conf/semanlink-config.xml&quot; file  to describe where you'll store the metadata, how it relates to URLs of your  site, and how it is loaded. It's easier to describe it through examples:</p><h3>Tagging static pages</h3><p>Suppose you want to use Semanlink to tag static pages    served from a directory C:\site\statix at URLs under &quot;http://www.yourdomain.com/statix&quot;.</p><p>Open the &quot;[semanlinkDataDir]/conf/semanlink-config.xml&quot; file    and add a slc:dataFolder property to the sl:SLModel defined like that:</p><pre><code>		&lt;slc:dataFolder&gt;<br>			&lt;sl:DataFolder rdf:about=&quot;http://www.yourdomain.com/statix&quot;&gt;<br>				&lt;slc:filePath&gt;C:\site\statix&lt;/slc:filePath&gt;<br>			&lt;/sl:DataFolder&gt;<br>		&lt;/slc:dataFolder&gt;</code></pre><p>You'll then be able to add metadata to the files inside &quot;statix&quot;, and this  metadata will be saved in &quot;sl.rdf&quot; files along them (See <a href="config.htm">semanlink-configuration</a> for  more information.)</p><h3>Tagging generated pages</h3><p>Tagging pages generated dynamically is not very different. To tag pages served  under &quot;http://www.yourdomain.com/webapp&quot;, create a DataFolder such as:</p><pre><code>		&lt;slc:dataFolder&gt;<br>			&lt;sl:DataFolder rdf:about=&quot;http://www.yourdomain.com/webapp&quot;&gt;<br>				&lt;slc:filePath&gt;C:\site\webapp_metadata&lt;/slc:filePath				&lt;slc:loadingMode&gt;simple&lt;/slc:loadingMode&gt;<br>			&lt;/sl:DataFolder&gt;<br>		&lt;/slc:dataFolder&gt;</code></pre><p>All metadata will be stored in the file &quot;C:\site\webapp_metadata\sl.rdf&quot;</p></div>